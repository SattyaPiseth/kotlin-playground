# E02 — Classify Sign — Quiz Me (5 Questions)

1. Why is `when { ... }` the right style for range/condition checks instead of `when (value)` here?
2. Why is an `else` branch appropriate when classifying an `Int` sign?
3. What changes if the function returns an enum instead of `String` labels?
4. How would you test boundary values for sign classification, and why do they matter?
5. When should you keep KDoc for a simple function like this, and what should it contain?
