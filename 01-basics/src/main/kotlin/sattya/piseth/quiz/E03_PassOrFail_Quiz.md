# E03 — Pass or Fail — Quiz Me (5 Questions)

1. Why is `when` more suitable than a single `if` once validation is introduced?
2. What is the benefit of throwing `IllegalArgumentException` for scores outside `0..100`?
3. Why should tests include both threshold values (`49`, `50`) and domain boundaries (`0`, `100`)?
4. When would returning a typed result be better than returning `"PASS"`/`"FAIL"` strings?
5. What minimal KDoc fields are important when a function enforces input constraints?
