# E01 — Max of Two — Quiz Me (5 Questions)

1. Why is expression-style `if` preferable to assigning into a mutable `result` variable?
2. In `if (a > b) a else b`, what happens when `a == b`, and why is that still correct?
3. When would you choose an explicit return type over inference in small functions?
4. How would the contract change if parameters were nullable (`Int?`)?
5. What boundary inputs are essential for integer comparison functions, and why?
