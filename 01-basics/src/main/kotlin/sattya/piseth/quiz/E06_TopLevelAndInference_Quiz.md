# E06 — Top-Level and Inference — Quiz Me (5 Questions)

1. Why is `isBlank()` a better validation than `isEmpty()` for user-facing names?
2. What is the tradeoff between inferred and explicit return types in top-level functions?
3. Why is input normalization (`trim`) part of contract behavior, not just formatting?
4. When would you prefer a block body over a single-expression body here?
5. What tests prove both valid formatting and invalid-input behavior for `greet`?
