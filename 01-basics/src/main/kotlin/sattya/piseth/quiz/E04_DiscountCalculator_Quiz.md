# E04 — Discount Calculator — Quiz Me (5 Questions)

1. Why is an enum-based `when` safer than using raw membership strings?
2. Why should `NaN` and infinity be rejected in price calculations?
3. What is the purpose of using a tolerance in floating-point assertions?
4. Why can `when (membership)` omit `else` here?
5. How would you extend the API safely if a new membership tier is added later?
