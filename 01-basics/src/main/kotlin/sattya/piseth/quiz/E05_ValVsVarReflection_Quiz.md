# E05 — Val vs Var Reflection — Quiz Me (5 Questions)

1. Why is `var` justified in this exercise even under an immutability-first style?
2. What does `val` guarantee, and what does it not guarantee?
3. Why does an expression body with `run` still count as expression-oriented Kotlin?
4. What kinds of tests are useful for deterministic string-format exercises?
5. How would this example change if you wanted to demonstrate object mutability vs reference immutability?
