# Module 01 — Kotlin Basics (P0)

## 🎯 Goals
- Understand `val` vs `var`
- Write top-level functions
- Use `if` and `when` as expressions
- Understand expression-oriented programming

## 🧠 Concepts to Master
- Variables (`val`, `var`)
- Type inference
- Top-level functions
- `if` as expression
- `when` expression

## 🧪 Exercises
1. Create a function that returns the max of two numbers using `if` as expression.
2. Write a `when` expression to classify a number (negative, zero, positive).
3. Refactor Java-style code into Kotlin expression style.

## 🚀 Mini Challenge
Create a simple discount calculator:
- Input: price and membership level
- Output: final price after discount

Use `when` expression only.

## ✅ Completion Checklist
- [x] I can explain difference between `val` and `var`
- [x] I use `if` as expression naturally
- [x] I no longer write Java-style verbose Kotlin
