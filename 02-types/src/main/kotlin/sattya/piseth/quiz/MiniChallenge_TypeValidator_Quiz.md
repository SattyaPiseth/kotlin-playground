# Mini Challenge — Type Validator — Quiz Me (5 Questions)

1. Why is `Any` a good input type for this mini challenge, and what does it exclude?
2. How does nested `when` improve readability when validation depends on runtime type?
3. What is the tradeoff between returning formatted strings and a typed validation result object?
4. Why should non-finite doubles be treated differently from finite negative doubles?
5. How would you extend support for nullable input (`Any?`) without using `!!`?
