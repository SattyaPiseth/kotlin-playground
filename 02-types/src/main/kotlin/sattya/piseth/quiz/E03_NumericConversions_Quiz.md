# E03 — Numeric Conversions — Quiz Me (5 Questions)

1. Why does Kotlin require `toLong()` when adding `Int` and `Long`?
2. What bug classes does explicit numeric conversion prevent compared with implicit widening?
3. Why is `Number` useful for classification but not ideal for arithmetic APIs?
4. How would you test overflow behavior separately from conversion behavior?
5. When should numeric APIs return `Long` vs `Int` vs `Double`?
