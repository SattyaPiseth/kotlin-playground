# E01 — Type Classifier — Quiz Me (5 Questions)

1. Why does `when (input)` with `is` branches enable smart casts inside each branch?
2. What is the difference between `input is String` and `input::class == String::class` for this exercise?
3. Why is `Any` non-null in Kotlin, and how would behavior change with `Any?`?
4. When is it appropriate to return formatted strings vs a sealed result type?
5. What edge cases matter when classifying strings, integers, and doubles?
